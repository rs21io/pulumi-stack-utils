name: tests

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'
          cache: 'pip'
      - name: Install packages
        run: pip install -r requirements.txt
      - name: Run tests
        run: py.test test/ --pylint --cov ./
